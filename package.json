{
  "name": "authai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter './apps/*' dev",
    "dev:relay": "pnpm --filter @authai/relay-server dev",
    "dev:example": "pnpm --filter example-react dev",
    "dev:backend": "pnpm --filter example-backend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
